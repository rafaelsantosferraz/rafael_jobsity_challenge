import 'package:flutter/material.dart';

const double kDefaultPadding = 16;
const Widget kVerticalGap = SizedBox(width: 0, height: kDefaultPadding);
const BoxShadow kDefaultShadow = BoxShadow(
  offset: Offset(0, 4),
  blurRadius: 4,
  color: Colors.black26,
);

const kAnimationDuration = 500;